using Microsoft.UI.Xaml.Controls;

namespace SnakeUi.MVVM.View.Settings;

public sealed partial class AudioSettings : UserControl
{
    public AudioSettings()
    {
        this.InitializeComponent();
    }
}
